services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./config.py:/app/config.py
      - ./docs:/app/docs
    command: >
      python -m uvicorn src.api.main:app
      --host 0.0.0.0
      --port 8000
      --reload
